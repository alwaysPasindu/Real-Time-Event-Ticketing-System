module.exports = {
  totalTickets: 1000,          // Initial total tickets available
  ticketReleaseRate: 10,       // Tickets released per unit time (by vendor)
  customerRetrievalRate: 5,    // Tickets bought per unit time (by customer)
  maxTicketCapacity: 1000      // Max capacity for tickets in the system
};
